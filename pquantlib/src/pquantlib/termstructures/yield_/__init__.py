"""Yield term structures — concrete curves derived from YieldTermStructure.

# C++ parity: ql/termstructures/yield/*.hpp (v1.42.1)

This subpackage is named ``yield_`` (trailing underscore) because
``yield`` is a Python keyword and cannot appear as a module name.
The C++ namespace is ``QuantLib::`` rooted under
``ql/termstructures/yield/`` — same content, escaped name.
"""
