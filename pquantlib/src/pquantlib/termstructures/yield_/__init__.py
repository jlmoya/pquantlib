"""Yield-curve term structures + rate helpers.

# C++ parity: ql/termstructures/yield/*.hpp (v1.42.1)

The directory is named ``yield_`` (trailing underscore) because ``yield``
is a Python keyword and cannot appear as a module name. Same content as
the C++ namespace rooted at ``QuantLib::`` under ``ql/termstructures/yield/``.
"""
